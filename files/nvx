#!/usr/bin/env bash
set -euo pipefail

usage() { echo "Usage: nvx <command> [args...]" >&2; exit 2; }
[[ $# -ge 1 ]] || usage

# Run via sudo on host and inside containers (dbx-cmd handles this)
export DBX_CMD_SUDO=1

exec dbx-cmd "$@"
