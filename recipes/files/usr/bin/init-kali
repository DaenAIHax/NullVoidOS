#!/bin/bash
# Script per inizializzare Kali Linux su NullVoidOS

echo "🛠️  Sto preparando il tuo ambiente di hacking..."

# 1. Crea il container (senza chiedere conferme con -Y)
distrobox create --image docker.io/kalilinux/kali-rolling --name kali -Y

# 2. Installa subito i tool base dentro Kali
echo "🚀 Installazione Metasploit e tool base..."
distrobox enter kali -- sudo apt-get update
distrobox enter kali -- sudo apt-get install -y metasploit-framework enum4linux seclists wordlists exploitdb

echo "✅ Fatto! Per entrare in Kali digita: distrobox enter kali"
