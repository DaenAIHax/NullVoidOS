[package]
name = "null"
version = "0.1.0"
edition = "2021"
description = "NullVoidOS system description language CLI"
license = "MIT"

[lib]
name = "null"
path = "src/lib.rs"

[[bin]]
name = "null"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
